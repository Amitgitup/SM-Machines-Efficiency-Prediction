apiVersion: v1
kind: Service
metadata: 
  name: my-service
spec: 
  selector: 
    app: mlops-app
  ports: 
    - port: 80
      targetPort: 5000
  type: NodePort

  
## 80 is the port where the service is exposed, while 5000 is the port where the application is running
